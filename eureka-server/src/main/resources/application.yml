server:
  port: 8001
spring:
  application:
    name: eureka-server
eureka:
  client:
    register-with-eureka: false #不向注册中心注册自己
    fetch-registry: false #不从注册中心取服务
  instance:
    hostname: localhost
  server:
    enable-self-preservation: false #关闭保护模式
